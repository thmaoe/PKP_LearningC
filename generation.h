#ifndef GENERATION_H
#define GENERATION_H

#include "constants.h"

int* generateA(void); //generate matrix A, with shape from the paper.

int* generateV(void); //generate random vector.

int* generatePermutation(void); //generate random permutation

#endif
